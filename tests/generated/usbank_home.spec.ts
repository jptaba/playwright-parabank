import { test } from '../../utils/bdd/playwrightAdapter';

test('Homepage elements are visible', async ({ open, fill, click, shouldSee, fillElement, clickElement, shouldSeeElement }) => {
    // UNMAPPED: I am on the US Bank homepage
    // UNMAPPED: the header should be visible
    // UNMAPPED: the footer should be visible
});

