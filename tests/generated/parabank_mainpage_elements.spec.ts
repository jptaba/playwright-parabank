import { test } from '../../utils/bdd/playwrightAdapter';

test('All main page elements are present', async ({ open, fill, click, shouldSee, fillElement, clickElement, shouldSeeElement }) => {
    // UNMAPPED: I am on the Parabank main page
    // UNMAPPED: all main page elements should be present and visible
});

